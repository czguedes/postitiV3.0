import { Container, Typography } from '@mui/material';
import React from 'react';

export const Home: React.FC = () => {
	return (
		<>
			<Container>
				<Typography>Postiti</Typography>
			</Container>
		</>
	);
};
